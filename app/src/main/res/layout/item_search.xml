<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView android:id="@+id/mcvItemSearch"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginHorizontal="12dp"
    android:layout_marginVertical="6dp"
    app:cardBackgroundColor="@color/white"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp"
    xmlns:tools="http://schemas.android.com/tools">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/clItemSearch"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp">

        <ImageView
            android:id="@+id/ivGameCoverSearch"
            android:layout_width="72dp"
            android:layout_height="108dp"
            android:scaleType="centerCrop"
            android:contentDescription="@string/cover"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvGameTitleSearch"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="8dp"
            android:textColor="@color/green"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="2"
            android:ellipsize="end"
            app:layout_constraintEnd_toStartOf="@+id/llButtonContainerSearch"
            app:layout_constraintStart_toEndOf="@+id/ivGameCoverSearch"
            app:layout_constraintTop_toTopOf="@+id/ivGameCoverSearch"
            tools:text="Játék neve"/>

        <TextView
            android:id="@+id/tvGameYearSearch"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/black"
            android:textSize="14sp"
            app:layout_constraintEnd_toEndOf="@+id/tvGameTitleSearch"
            app:layout_constraintStart_toStartOf="@+id/tvGameTitleSearch"
            app:layout_constraintTop_toBottomOf="@+id/tvGameTitleSearch"
            tools:text="Kiadási év"/>

        <TextView
            android:id="@+id/tvGamePublisherSearch"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@color/black"
            android:textSize="14sp"
            app:layout_constraintEnd_toEndOf="@+id/tvGameTitleSearch"
            app:layout_constraintStart_toStartOf="@+id/tvGameTitleSearch"
            app:layout_constraintTop_toBottomOf="@+id/tvGameYearSearch"
            tools:text="Kiadó"/>

        <LinearLayout
            android:id="@+id/llButtonContainerSearch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageButton
                android:id="@+id/btnAddLibrarySearch"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/add_to_library"
                android:src="@drawable/ic_add"
                app:tint="@color/green" />

            <ImageButton
                android:id="@+id/btnAddWishlistSearch"
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:layout_marginTop="8dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="@string/add_to_wishlist"
                android:src="@drawable/ic_wishlist"
                app:tint="@color/green" />

        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>